# B1 泛降临回退卡

用途：主题有明显的下落、降临、打击倾向，但信息还不够具体。

策略：
- 这是过渡卡，不直接进入完整模板生成
- 一旦命中 B1，优先回退到 B2 继续生成

适用信号：
- 只有模糊的降落感或压制感
- 还不能稳定判断为其他更明确的结构类模板
- 语义整体仍更接近覆盖式垂直打击，而不是容器类

输出要求：
- selected_template 返回 B1
- fallback_needed 返回 true
- fallback_target 返回 B2
